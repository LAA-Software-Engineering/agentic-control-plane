apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: audit-tamper
spec:
  imports:
    - ./policies/cheap-ceiling.yaml
  defaults:
    policy: cheap-ceiling
    model: mock/gpt-4
    runtime: local
  providers:
    models:
      mock:
        type: mock
