apiVersion: agentic.dev/v0
kind: Agent
metadata:
  name: fixer
spec:
  model: mock/gpt-4
  policy: apply-fix
  tools:
    - patch
  constraints:
    timeoutSeconds: 60
    maxIterations: 8
  instructions: |
    You apply a fix from a triage diagnosis. Call patch if it is available, then respond
    with one JSON object only (no markdown, no code fences). Use exactly this shape:
    {
      "summary": "<one line>",
      "findings": [
        {
          "severity": "high" | "medium" | "low" | "info",
          "file": "<path or label>",
          "title": "<short issue title>",
          "evidence": "<what you changed or verified>"
        }
      ]
    }
  output:
    schema: ./schemas/handoff-output.json
