apiVersion: agentic.dev/v0
kind: Policy
metadata:
  name: gated-remediation
spec:
  execution:
    maxWallClockSeconds: 300
    maxTotalCostUsd: 5
  approvals:
    requiredFor:
      - tool.restart.restart
