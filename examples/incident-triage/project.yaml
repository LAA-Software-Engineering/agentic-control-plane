apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: incident-triage
spec:
  imports:
    - ./policies/gated-remediation.yaml
    - ./tools/pager.yaml
    - ./tools/logs.yaml
    - ./tools/tracker.yaml
    - ./tools/restart.yaml
    - ./agents/responder.yaml
    - ./workflows/incident-triage.yaml
  defaults:
    policy: gated-remediation
    model: mock/gpt-4
    runtime: local
  providers:
    models:
      mock:
        type: mock
