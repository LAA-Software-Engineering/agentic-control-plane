apiVersion: agentic.dev/v0
kind: Policy
metadata:
  name: gated-publish
spec:
  execution:
    maxWallClockSeconds: 300
    maxTotalCostUsd: 5
  approvals:
    requiredFor:
      - tool.publish.default
