apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: regression-test
spec:
  imports:
    - ./policies/gated-publish.yaml
    - ./tools/publish.yaml
    - ./agents/writer.yaml
    - ./workflows/gated-write.yaml
  defaults:
    policy: gated-publish
    model: mock/gpt-4
    runtime: local
  providers:
    models:
      mock:
        type: mock
