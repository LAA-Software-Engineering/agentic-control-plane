apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: validate-lint-sensitive
spec:
  imports:
    - ./tool.yaml
    - ./policy.yaml
    - ./workflow.yaml
  defaults:
    policy: default
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
