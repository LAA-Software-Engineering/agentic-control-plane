apiVersion: agentic.dev/v0
kind: Workflow
metadata:
  name: pr-review
spec:
  policy: guarded-writes
  steps:
    - id: fetch_pr
      uses: tool.github.read_pr
    - id: review
      agent: reviewer
