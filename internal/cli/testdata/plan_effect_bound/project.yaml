apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: plan-effect-bound
spec:
  imports:
    - ./policy.yaml
    - ./tool.yaml
    - ./agent.yaml
    - ./workflow.yaml
  defaults:
    policy: guarded-writes
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
