apiVersion: agentic.dev/v0
kind: Agent
metadata:
  name: reviewer
spec:
  model: mock/gpt-4
  policy: guarded-writes
  tools:
    - tool.github.post_comment
  instructions: |
    You review pull requests.
