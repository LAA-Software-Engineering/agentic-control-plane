apiVersion: agentic.dev/v0
kind: Policy
metadata:
  name: guarded-writes
spec:
  execution:
    maxTotalCostUsd: 3
  effects:
    permit:
      - github.read
      - github.write
      - external.visible
