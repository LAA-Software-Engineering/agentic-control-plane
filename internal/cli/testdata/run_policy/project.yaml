apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: polproj
spec:
  defaults:
    policy: gate
  imports:
    - ./tools.yaml
    - ./workflow.yaml
    - ./policy.yaml
  providers:
    models:
      mock:
        type: mock
