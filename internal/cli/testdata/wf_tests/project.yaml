apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: wf-tests
spec:
  imports:
    - ./policy.yaml
    - ./tool.yaml
    - ./workflow.yaml
  defaults:
    policy: default
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
