apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: validate-lint-switch
spec:
  imports:
    - ./tool.yaml
    - ./policy.yaml
  defaults:
    policy: default
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
