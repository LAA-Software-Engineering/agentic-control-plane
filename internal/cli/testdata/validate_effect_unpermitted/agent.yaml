apiVersion: agentic.dev/v0
kind: Agent
metadata:
  name: deploy-agent
spec:
  model: mock/gpt-4
  policy: staging-only
  tools:
    - tool.kubernetes.restart
  instructions: |
    Restart the service when asked.
