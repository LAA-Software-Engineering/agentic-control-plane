apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: validate-ok
spec:
  imports:
    - ./tool.yaml
    - ./policy.yaml
    - ./workflow.yaml
    - ./env.yaml
  defaults:
    policy: default
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
