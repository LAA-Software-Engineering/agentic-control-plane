apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: safetyproj
spec:
  imports:
    - ./policy.yaml
    - ./tools.yaml
    - ./workflow.yaml
  providers:
    models:
      mock:
        type: mock
