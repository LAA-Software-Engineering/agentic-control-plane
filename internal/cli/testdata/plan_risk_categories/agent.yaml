apiVersion: agentic.dev/v0
kind: Agent
metadata:
  name: reviewer
spec:
  model: mock/gpt-4
  policy: default
  tools:
    - helper
  constraints:
    timeoutSeconds: 60
  instructions: |
    You review changes.
