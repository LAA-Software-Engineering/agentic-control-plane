apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: plan-risk-fixture
spec:
  imports:
    - ./policy.yaml
    - ./helper.yaml
    - ./github.yaml
    - ./agent.yaml
  defaults:
    policy: default
    model: mock/gpt-4
  providers:
    models:
      mock:
        type: mock
