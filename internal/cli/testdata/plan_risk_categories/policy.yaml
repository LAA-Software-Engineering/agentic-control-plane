apiVersion: agentic.dev/v0
kind: Policy
metadata:
  name: default
spec:
  execution:
    maxTotalCostUsd: 3
    maxWallClockSeconds: 60
  approvals:
    requiredFor:
      - tool.helper.echo
      - tool.github.issues.write
