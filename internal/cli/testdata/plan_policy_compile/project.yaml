apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: policy-compile-fixture
spec:
  defaults:
    policy: default
  imports:
    - ./policy.yaml
    - ./helper.yaml
    - ./reader.yaml
    - ./shell.yaml
