apiVersion: agentic.dev/v0
kind: Workflow
metadata:
  name: gated
spec:
  policy: gate
  steps:
    - id: publish
      uses: tool.helper.echo
      with:
        topic: "${input.topic}"
  output:
    value:
      published: "${steps.publish.output.echo.topic}"
