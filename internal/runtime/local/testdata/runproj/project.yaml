apiVersion: agentic.dev/v0
kind: Project
metadata:
  name: runproj
spec:
  imports:
    - ./policy.yaml
    - ./tools.yaml
    - ./agents.yaml
    - ./workflows.yaml
    - ./environments.yaml
  providers:
    models:
      mock:
        type: mock
  traces:
    backend: sqlite
    retentionDays: 30
